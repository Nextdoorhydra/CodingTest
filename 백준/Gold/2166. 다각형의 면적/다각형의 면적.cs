var A = Enumerable.Range(1, int.Parse(Console.ReadLine())).Select(x => Console.ReadLine().Split().Select(double.Parse).ToList()).ToList();
Console.WriteLine($"{Math.Abs(A.Append(A[0]).Aggregate((S: 0d, v: (List<double>)null), (pre, v) => pre.v == null ? (0d, v) : (pre.S + 0.5 * (pre.v[0] * v[1] - pre.v[1] * v[0]), v)).S):F1}");